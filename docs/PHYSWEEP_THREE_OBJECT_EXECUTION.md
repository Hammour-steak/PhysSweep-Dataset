# 3obj v5 执行与验收记录

> 历史开发记录：下文“等待配额”“未开始”等描述保留当时状态。
> r7 正式生产与整理已完成，当前状态见 [代码与发布说明](PHYSWEEP_CODE_STATUS.md)。

按开发文档逐阶段执行。冻结的阶段源码、输入与证据保留；本记录单独更新，不改写冻结快照。现有 1obj/2obj 正式数据不在本次修改范围。

| 阶段 | 状态 | 正确性检查 | 文档对齐 |
|---|---|---|---|
| 基线 | 通过 | 保存原始 366 文件；原始 CI 402 项通过 | §11.3 独立源码与旧行为回归 |
| D0 | 通过、已冻结 | 来源哈希链与五份规则配置；CI 411 项 | §3/4/7/11 四模板、角色、单目标与资源绑定 |
| D1 | 通过、已冻结 | 单组完整 13 条；433 项 CI；正式尺寸视频全部解码；共同发布验收；347 文件恢复不变；人工关键帧与组内首帧检查 | §5–9、§12.1 最小闭环，仅一个三球传递场景 |
| D2 r1 | 保留，未放行 | 120 个 metadata 生成成功、439 项 CI 通过；额外对齐审查发现物理签名混入执行设置 | §4.3、§13 要求仅改变 solver 不增加唯一物理数，因此修正后使用新源码及 work ID |
| D2 r2 | 通过、已冻结 | 440 项 CI；120 唯一物理候选；实际派生 1560 条内存输入并验证 13 条组；来源与纹理哈希、角色、初态、文本绑定正确；恢复 128 文件不变 | §12.D2、§14 分开报告物理覆盖与视觉请求；无额度转移；近似距离仅作诊断 |
| D3 r2 | 通过、已冻结 | 120 候选、46 通过、598 完整组成员；逐组输入/事件/运行时物性/轨迹检查；3,727 文件恢复不变 | §6/7/12.D3：552 sweep 中 11 条改变接触图、167 条最大位置差小于 0.1mm，均按完整性保留 |
| D2 r3 构造迭代 | 通过、已冻结 | 442 项 CI；120 metadata、1560 内存派生、128 文件恢复不变；两轮重合 30 个物理状态，合并唯一数 210 | 仅修正多次碰撞初态构造；未降低准入规则或改物性；重复对照不算新增覆盖 |
| D3 r3 | 通过、已冻结 | 120 候选、104 通过、1352 完整组成员；逐组复核通过；7,497 文件恢复不变；两轮相同的 30 场景逐元素复现 | 四模板分别 16/30、30/30、30/30、28/30 通过；数值与 sweep 完整性无未解释失败；202 条很小响应也保留 |
| D4 | 通过、已冻结 | 复用绑定/渲染/发布流程；8 个预选完整组；同模板有限相机候补；444 项 CI 通过 | 先冻结 base-only 选组，再读取 sweep；新的视觉版本绑定旧物理源码及规则；无正式生产 |
| D5 | 通过、已冻结 | 完成混合形状、斜面、primitive/compound 资产、exact-mesh 支撑、台球、被动弹珠台和三段轨道的逐分支物理及媒体证据 | 每个资产或专用场景只保留已验收 cell；不从名义笛卡尔积扩张 |
| D6 | 规则范围已收缩并完成完整组验收，等待配额 | 四个顶层 family：generic、billiards、pinball、marble_run；generic 只保留 flat、inclined、exact_mesh_support 三个子场景；多 mesh 仅进入 pair-control，且每个场景最多一个真实非球资产；完整 13 条 multi-mesh 组的物理、固定相机和正式尺寸渲染全部通过 | §8.4/8.5、§12.D6：物体占比双边受限，仅非关键帧允许少量出画；规则文件不写生产数量；正式采样未启动 |
| D7 | 未开始 | 等待用户确认 1,538 个 base 的分支及分支内配额 | 只有配额冻结后才生成正式 metadata 与启动生产 |

D2 r2 覆盖：四模板 × 两类平面 host × 三类干预角色 × 五种角色尺度组合，共 120 候选。使用 263 个物体来源、111 个 host 来源、22 种对象视觉资产；251 个实际素材文件通过哈希检查。单物体来源最多使用 3 次，host 最多 2 次。三个请求相机族在每个模板各 10 个候选；这些是请求值，不能提前当成最终视频覆盖。

D2 r2 首轮限制：三球、两个平面支撑类别、五个相对尺度组合、固定共同光照预设。该限制只描述当时的首轮，不再代表 D6 的最终支持范围；后续 D5 已分别验收混合形状、斜面、台球与其他专用 family。

D6 运动补充：真实物性试验覆盖斜向 chain、错位 successive 和穿越 pair-control。16 个候选中 14 个通过，形成 182 条完整成员并完成 14 个正式尺寸 base 视频；两个右偏 angled-chain 因缺少 Q→R 第二次传递被排除。斜面沿用独立的 sphere pair-control 规则；新域 24 个候选中 16 个通过，形成 208 条完整成员并覆盖全部 12 个 host×scale×target 单元，16 个 base 固定相机均通过占比与事件可见性审计，另完成 4 个正式尺寸代表视频，不继承新的平面布局。精确允许项见 `configs/three_object_rule_matrix.json` 与 `configs/three_object_motion_scope_evidence.json`。

D2 r2 工作标识：`three_object_d2_pilot_20260907_r2`。

代码 SHA256：`4b194322ffb48b75caacb472b5e18ba26ceb9b88870d4e428c1b3650f4bc8034`。

规则 SHA256：`da70e65d0244881b18d41c51dad29475cda6843025929ef07f027dacaa8cf01b`。

验收证据保存在服务器独立审计目录，入口为 `d0_freeze.json`、`d1_gate.json`、`d2_gate.json`；每个 gate 引用实际证据与源码归档哈希。当前 D3 沿用已冻结的 D2 r2 源码和候选，尚未写入正式发布目录。

D3 构造诊断：r2 中连锁传递 1/30、汇入 5/30 通过，失败来自缺少必需接触。独立诊断 v1 对 6 个失败连锁样本尝试两个初始速度，共 12 个样本，7 个通过；v2 在四模板各 6 个来源组合上固定 1.2 m/s、联合计算间距，共 24 个尝试，连锁 5/6、汇入 6/6、连续碰撞 6/6 通过，对照 1 个通过、5 个初态布局超出支撑。诊断只调整完整初态，不中途施力、不替换原样本。

因此 r3 仅为三个多次碰撞模板使用 1.1–1.3 m/s 与 0.26–0.34 秒名义首接触时间联合构造间距，间距仍限于 D0 的 1.5–4 倍范围；对照模板保留原方案。使用新的源码、work ID、计划和独立 gate。实际接触时间及传递仍由物理准入验证，名义时间不当作观测事实。

真实旧输入回归补充：1obj 球体和 2obj 方块撞球各用原始冻结版本与 r2 版本执行，全部轨迹数组逐元素一致，审计内容一致。证据 `legacy_physics_replay.json`；范围仅为这两类真实输入，不替代其他分支测试。
